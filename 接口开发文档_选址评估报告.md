# 选址评估报告接口开发文档

> 面向后端接口开发。目标是为前端「银行选址评估报告」提供数据接口，让前端把本地 `src/mock/reportData.js` 替换为接口返回，尽量做到视图层零改动。
>
> 技术栈：Spring Boot 2.7.18 + MyBatis + PostgreSQL/PostGIS。

---

## 1. 数据模型

### 1.1 主表：`tb_regional_score`

一行代表一次评估任务，也就是前端一份报告的主记录。

| 字段 | 类型 | 含义 | 接口用途 |
|---|---|---|---|
| `id` | bigint | 评估 ID | 入参 `regionalScoreId` |
| `name` | varchar | 区域名称或评估名称 | `meta.region`、`points[].region` 默认来源 |
| `type` | smallint | 区域类型：1 行政区域，2 自定义区域 | 元信息 |
| `province_code` | integer | 省份代码 | 预留，后续可转中文 |
| `city_code` | integer | 城市代码 | `meta.city`，有字典则转中文，无字典先返回代码 |
| `district_code` | varchar | 区县代码集合，分号分隔 | 预留，后续可转中文 |
| `collection_date` | varchar | 数据采集期 | `meta.period` |
| `model_id` | bigint | 模型 ID | `meta.model`，以及模型参数关联 |
| `status` | smallint | -1 失败，0 进行中，1 已完成 | 未完成时返回错误 |
| `model_param_json` | varchar | 模型参数 JSON | 解析维度名称、权重、子项说明 |
| `model_score_max_min` | varchar | 模型评分范围/阈值配置 | 后续用于动态分层入口 |
| `use_type` | smallint | 1 智能选址，2 点位评估 | 默认推断 `scope` |
| `geom` | geometry(POLYGON,4326) | 评估区域面 | 区域范围、面积、坐标兜底来源 |
| `geom_mul_point` | geometry(MULTIPOINT,4326) | 评估点位多点 | 当前点位坐标来源 |
| `create_time` / `update_time` | timestamp | 创建/更新时间 | `meta.generatedAt` |

当前数据库已确认：表存在，但暂无数据；未建主键、外键、索引约束，代码中不能依赖数据库约束。

### 1.2 明细表：`tb_regional_score_grid`

一行代表一次评估下的一个网格点位。

| 字段 | 类型 | 含义 | 接口用途 |
|---|---|---|---|
| `grid_id` | bigint | 网格 ID | `points[].code`，单点入参 `pointIds[]` |
| `regional_score_id` | bigint | 关联 `tb_regional_score.id` | 查询点位明细 |
| `name` | varchar | 网格名称 | 展示备用字段 |
| `grid_indicator_score` | numeric | 综合得分 | `points[].score`、排序、分层 |
| `grid_pop_score` | numeric | 人口维度分 | `points[].dims.pop` |
| `grid_peer_score` | numeric | 同业竞争维度分 | `points[].dims.peer` |
| `grid_resource_score` | numeric | 资源维度分 | `points[].dims.resource` |
| `grid_traffic_score` | numeric | 交通设施维度分 | `points[].dims.traffic` |
| `grid_road_score` | numeric | 道路种类维度分 | `points[].dims.road` |
| `create_time` / `update_time` | timestamp | 创建/更新时间 | 预留 |

逻辑关联：`tb_regional_score.id = tb_regional_score_grid.regional_score_id`。数据库暂未建外键，Mapper 按字段显式关联。

---

## 2. 接口定义

### 2.1 URL

`POST /api/site-report/data`

### 2.2 入参

```json
{
  "regionalScoreId": 1001,
  "scope": "multi",
  "box": "white",
  "pointIds": [330101001001]
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `regionalScoreId` | long | 是 | 评估 ID，对应 `tb_regional_score.id` |
| `scope` | string | 否 | `multi` 多点报告，`single` 单点报告；为空时按 `use_type` 推断：1 -> `multi`，2 -> `single` |
| `box` | string | 否 | `white` / `black`；后端按方案 A 始终返回完整数据，显隐交前端 |
| `pointIds` | array<long> | 条件 | 指定点位网格 ID。`single` 取一个；`multi` 可传多个用于过滤，不传则返回该评估下全部点位 |

入参规则：

1. `regionalScoreId` 必须存在。
2. `scope=single` 时：
   - `pointIds` 传 1 个则返回该点位。
   - `pointIds` 为空则取该评估下 `grid_indicator_score` 最高的点位。
   - `pointIds` 多于 1 个视为入参错误。
3. `scope=multi` 时：
   - `pointIds` 为空返回该评估下全部点位。
   - `pointIds` 非空只返回这些网格点位。
4. `box` 只透传，不裁剪字段。

### 2.3 响应

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "meta": {},
    "dims": [],
    "tiers": [],
    "points": [],
    "single": null
  }
}
```

---

## 3. 出参结构

顶层结构固定为：

```json
{
  "meta": {},
  "dims": [],
  "tiers": [],
  "points": [],
  "single": null
}
```

`single` 仅在 `scope=single` 时返回对象；多点报告返回 `null` 或不返回该字段，以前端 mock 契约为准。

### 3.1 `meta`

| 字段 | 来源 | 规则 |
|---|---|---|
| `reportTitle` | `use_type` / `scope` | `multi` -> `全量点位评估报告`；`single` -> `单点点位评估报告` |
| `city` | `city_code` | 有字典时转中文；无字典时先返回 `city_code` 字符串 |
| `region` | `tb_regional_score.name` | 默认直接使用主表 `name` |
| `model` | `model_id` / `model_param_json` | 先返回 `model_id` 字符串；后续如有模型表再转模型名 |
| `period` | `collection_date` | 原样返回，后续再格式化 |
| `count` | grid 行数 | 当前响应内 `points.length` |
| `area` | `geom` | 有需要时用 PostGIS 计算；本期可为空 |
| `gap` | 暂无明确来源 | 本期可为空 |
| `regionSubtitle` | `district_code` / `name` | 本期优先为空或使用 `name` |
| `generatedAt` | `update_time` / `create_time` | 优先 `update_time`，为空用 `create_time` |

### 3.2 `dims[]`

维度来自 `tb_regional_score_grid` 的 5 个评分列。代码中用维度配置列表循环处理，不在业务逻辑中散落硬编码字段名。

```json
[
  {
    "key": "pop",
    "name": "人口规模评分",
    "weight": 37.75003385,
    "desc": "",
    "src": "",
    "min": 75.8,
    "max": 97.3,
    "avg": 86.4
  }
]
```

| key | grid 列 | 默认中文名 | model_param_json 期望 `scoreCode` |
|---|---|---|---|
| `pop` | `grid_pop_score` | 人口规模评分 | `pop_score` |
| `peer` | `grid_peer_score` | 同业竞争评分 | `peer_score` |
| `resource` | `grid_resource_score` | 资源评分 | `resource_score` |
| `traffic` | `grid_traffic_score` | 交通设施评分 | `traffic_score` |
| `road` | `grid_road_score` | 道路种类评分 | `road_score` |

`model_param_json` 解析规则：

```json
{
  "modelParamJson": [
    {
      "id": 323,
      "customWeight": "37.75003385",
      "scoreName": "人口规模评分",
      "scoreCode": "pop_score",
      "modelId": 17,
      "orderNum": 0,
      "parentId": null,
      "analysisItem": 1,
      "list": [
        {
          "id": 324,
          "customWeight": 3,
          "scoreName": "客流密度",
          "scoreCode": "pedestrian_density_score",
          "parentId": 323,
          "list": []
        }
      ]
    }
  ]
}
```

1. 根数组取 `modelParamJson`。
2. 顶层节点按 `scoreCode` 匹配五个维度：`pop_score`、`peer_score`、`resource_score`、`traffic_score`、`road_score`。
3. `dims[].name` 优先使用顶层节点 `scoreName`，没有则用默认中文名。
4. `dims[].weight` 使用顶层节点 `customWeight`，统一转 `BigDecimal`/`Double`。
5. 顶层节点的 `list` 是该维度的子指标配置，可用于白盒说明或单点洞察。
6. `desc`/`src` 当前无稳定字段来源，先返回空字符串；后续如前端需要，可由 `list[].scoreName` 汇总生成说明。

### 3.3 `points[]`

点位等于网格行。

```json
[
  {
    "rank": 1,
    "code": "330101001001",
    "region": "杭州主城区",
    "score": 89.1,
    "coord": "120.123456 , 30.123456",
    "tierKey": "rec",
    "dims": {
      "pop": 97.3,
      "peer": 73.7,
      "resource": 93.0,
      "traffic": 88.3,
      "road": 80.1
    }
  }
]
```

| 字段 | 来源 | 规则 |
|---|---|---|
| `rank` | 排序结果 | 按 `grid_indicator_score` 降序，从 1 开始 |
| `code` | `grid_id` | 确认使用网格 ID，不使用 `name` |
| `region` | `tb_regional_score.name` | 目前不查网格所属区，默认使用主表区域名 |
| `score` | `grid_indicator_score` | 原始分值 |
| `coord` | `geom_mul_point` / `geom` | 坐标未入 grid 表；本期先从 `geom_mul_point` 拆点，若没有点则取 `geom` 中心点 |
| `tierKey` | `score` | 按分层阈值计算 |
| `dims` | grid 5 列 | key 固定为 `pop/peer/resource/traffic/road` |

坐标规则：

1. 优先从 `tb_regional_score.geom_mul_point` 拆出点。
2. 当前尚未确认多点顺序与 grid 行一一对应关系；实现入口预留 `resolvePointCoord(regionalScore, grid, index)`。
3. 若 `geom_mul_point` 为空或无法按顺序匹配，先取 `tb_regional_score.geom` 的中心点作为兜底坐标。
4. 待后续坐标入库或提供网格空间表后，改为按 `grid_id` 精确取网格中心点。

### 3.4 `tiers[]`

固定返回四层，即使某层没有点位也返回。

```json
[
  { "key": "pref", "name": "优选区", "range": "≥90分", "min": 90, "colorKey": "pref", "strategy": "", "count": 0, "avg": null },
  { "key": "rec", "name": "推荐区", "range": "85-89分", "min": 85, "colorKey": "rec", "strategy": "", "count": 0, "avg": null },
  { "key": "alt", "name": "备选区", "range": "80-84分", "min": 80, "colorKey": "alt", "strategy": "", "count": 0, "avg": null },
  { "key": "obs", "name": "观察区", "range": "<80分", "min": 0, "colorKey": "obs", "strategy": "", "count": 0, "avg": null }
]
```

默认阈值先写死为 `90/85/80`。评分范围是动态配置，代码必须预留配置入口：

```java
class TierRuleConfig {
    BigDecimal prefMin = new BigDecimal("90");
    BigDecimal recMin = new BigDecimal("85");
    BigDecimal altMin = new BigDecimal("80");
}
```

后续如 `model_score_max_min` 提供明确结构，再从该字段解析并覆盖默认阈值。

### 3.5 `single`

单点报告在 `scope=single` 时返回。

| 字段 | 来源 | 规则 |
|---|---|---|
| `defaultPointCode` | 选中的 `grid_id` | 和 `points[0].code` 一致 |
| `bankCompetition[]` | `model_param_json` / 后续数据源 | 当前两张表没有银行数量明细；本期默认空数组 |
| `insights[]` | `model_param_json.modelParamJson[].list` | 可用模型子项生成说明型洞察；没有稳定文案时默认空数组 |
| `verdictTitle` | 固定值 | `综合评价` |

说明：

1. `model_param_json` 能提供维度和子指标名称/权重，但当前 grid 表只有 5 个维度总分，没有每个子指标在点位上的得分。
2. 因此本期 `single.bankCompetition` 不伪造数量，默认返回 `[]`。
3. `single.insights` 可先返回 `[]`；如果前端必须展示，可基于选中点位最高/最低维度和 `model_param_json` 的子项名称生成说明，但这属于派生文案，不是数据库原始事实。

---

## 4. 业务规则

1. 查询主表：按 `regionalScoreId` 查 `tb_regional_score`。
2. 校验状态：如果 `status` 存在且不是 `1`，返回 `1002`。
3. 查询网格：按 `regional_score_id` 查 `tb_regional_score_grid`。
4. 点位过滤：如果传 `pointIds`，只保留这些 `grid_id`。
5. 排序：`points` 按 `grid_indicator_score` 降序，`rank=index+1`。
6. 维度统计：每个维度跨当前响应内所有点位计算 `min/max/avg`，`avg` 保留 1 位小数。
7. 分层统计：按 `tierKey` 计算 `count/avg`；空层返回 `count=0, avg=null`。
8. box：采用方案 A，后端始终返回完整数据，前端根据 `box` 控制显隐。
9. 不做无依据默认数据：银行竞争、客群洞察没有数据源时返回空数组，不编造占位内容。

---

## 5. 错误码

| code | 含义 |
|---|---|
| `0` | 成功 |
| `1001` | 入参校验失败，例如缺少 `regionalScoreId`、`scope=single` 但传了多个 `pointIds` |
| `1002` | `regionalScoreId` 不存在，或评估状态未完成 |
| `1003` | 指定的 `pointIds` 不属于该评估 |
| `5000` | 服务内部错误 |

---

## 6. 落地步骤

1. DTO：新增 `ReportData`、`ReportMeta`、`ReportDim`、`ReportTier`、`ReportPoint`、`ReportSingle`。
2. Mapper：新增 `RegionalScoreMapper.java/xml`。
   - `selectRegionalScoreById(Long id)`
   - `selectGridScores(Long regionalScoreId, List<Long> pointIds)`
   - 坐标查询可直接在主表 SQL 中用 PostGIS 输出 `geom_mul_point` / `geom` 的 WKT 或坐标数组。
3. Service：新增 `SiteReportService`。
   - 解析 `model_param_json`
   - 组装维度配置
   - 组装点位、分层、统计
   - 处理单点默认点位
4. Controller：新增 `SiteReportController`，暴露 `POST /api/site-report/data`。
5. 验证：用真实库空表、无效 ID、未完成状态、单点、多点、pointIds 过滤分别测一遍。

---

## 7. 仍需确认

下面这些不阻塞第一版接口开发，但会影响展示精度：

| # | 事项 | 当前处理 |
|---|---|---|
| 1 | `model_param_json` 其他四个顶层维度的 `scoreCode` 是否分别为 `peer_score/resource_score/traffic_score/road_score` | 先按此约定实现；不匹配时回退默认中文名和空权重 |
| 2 | `geom_mul_point` 中点的顺序是否与 `tb_regional_score_grid` 排序后一致 | 先预留解析入口；不可靠时使用 `geom` 中心点 |
| 3 | 城市/区县代码字典表 | 暂未发现；第一版返回代码字符串或主表 `name` |
| 4 | `model_score_max_min` 的结构 | 第一版使用默认阈值 `90/85/80`，代码留动态配置入口 |
| 5 | 单点报告是否必须展示银行竞争/客群洞察模块 | 第一版返回空数组，不造假数据 |

