# mybatis-pg-demo 开发文档

## 项目概述

基于 **Spring Boot 2.7.18 + MyBatis + PostgreSQL** 的网格人口画像指标计算演示项目。项目同时包含一个简单的 User CRUD 演示入口，以及一套完整的网格人口画像指标计算逻辑。

| 维度 | 说明 |
|------|------|
| 基础框架 | Spring Boot 2.7.18（最后一个支持 Java 8 的版本线） |
| ORM | MyBatis 2.3.2，XML 映射 |
| 数据库 | PostgreSQL（配置在 `application.yml`，默认连接 `192.168.100.102:5432/inm`） |
| Java 版本 | 1.8 |
| 打包方式 | Spring Boot fat JAR |

---

## 目录结构

```
test_project/
├── pom.xml                              # Maven 构建配置
├── README.md                            # 项目说明（当前为空）
├── 开发文档.md                           # 本文档
├── src/
│   ├── main/
│   │   ├── java/com/example/
│   │   │   ├── Main.java                # 启动类 + CRUD 演示入口
│   │   │   ├── entity/
│   │   │   │   └── User.java            # 用户实体
│   │   │   ├── mapper/
│   │   │   │   ├── UserMapper.java       # User CRUD 接口
│   │   │   │   ├── ConstantMapper.java   # 归一化常量计算接口
│   │   │   │   └── PopulationMapper.java # 人口画像指标计算接口
│   │   │   └── service/
│   │   │       ├── RegionScoreDataService.java  # 抽象基类（模板方法）
│   │   │       └── ExServiceImpl.java           # RR 类型得分实现
│   │   └── resources/
│   │       ├── application.yml           # Spring Boot 配置
│   │       ├── create.txt               # SQL 建表/数据计算脚本（4294行）
│   │       └── mapper/
│   │           ├── UserMapper.xml        # User SQL 映射
│   │           ├── ConstantMapper.xml    # 常量计算 SQL 映射
│   │           └── PopulationMapper.xml  # 人口画像指标 SQL 映射
```

---

## 核心功能模块

### 1. User CRUD 演示（Demo 入口）

**入口类**：`Main.java` — 实现 `CommandLineRunner`，应用启动后自动执行。

| 方法 | 说明 |
|------|------|
| `findAll()` | 查询全部用户 |
| `findById(id)` | 按 ID 查询 |
| `insert(user)` | 新增用户（返回自增主键） |
| `update(user)` | 更新用户 |
| `deleteById(id)` | 删除用户（默认注释） |

**对应 SQL**：`UserMapper.xml` — 映射 `user` 表，支持驼峰下划线自动映射（`map-underscore-to-camel-case: true`）。

---

### 2. 网格人口画像指标计算（核心业务）

这是项目的核心能力，由 **3 层架构** 组成：

#### Mapper 层

| Mapper | 职责 |
|--------|------|
| `ConstantMapper.java/.xml` | 预计算全局归一化常量——每格平均居住/工作人口（`SUM(num)/6340`），供密度计算时使用 |
| `PopulationMapper.java/.xml` | 按网格 ID 列表批量计算各项人口画像指标，每方法返回 `{code: grid_id, result: 计算值}` 的 `LinkedHashMap` 列表 |

#### 指标分类

指标分为 **三大类**：

**第一类：直接占比** — 数据源 `tb_grid_pop_attr1`（单表，无 JOIN）

| 方法 | 指标 | 计算逻辑 |
|------|------|----------|
| `calcAgeRatio` | 年龄35-54占比 | `(age_3544 + age_4554) / (gender_female + gender_male)` |
| `calcMaleRatio` | 男性占比 | `gender_male / (gender_female + gender_male)` |
| `calcCarRatio` | 有车占比 | `has_car / (gender_female + gender_male)` |
| `calcBachelorRatio` | 本科及以上占比 | `qualification_undergraduate_graduate / (gender_female + gender_male)` |

**第二类：比例还原** — 数据源 `tb_grid_pop_attr1` JOIN `tb_grid_personalized_portrait`

| 方法 | 指标 | 计算逻辑 |
|------|------|----------|
| `calcHomeOwnerRatio` | 有房占比 | `SUM(there_room) / SUM(gender_female + gender_male)` |
| `calcWhiteCollarRatio` | 白领占比 | `white_collar_general_staff / 还原后总人口` |
| `calcSeniorMiddleClassRatio` | 中产占比 | `senior_middle_class / 还原后总人口` |
| `calcSilverHairedRatio` | 银发占比 | `urban_silver_haired / 还原后总人口` |
| `calcFinancialManagementRatio` | 金融理财人群占比 | `financial_management / 还原后总人口` |
| `calcConsumeHighRatio` | 高消费占比 | `consume_high / 还原后总人口` |
| `calcHighEndIndividualRatio` | 高净值人群占比 | `hieg_end_individual / 还原后总人口` |

> 第二类指标的还原逻辑：分母 = `画像值 / 画像占比`（当占比为 0 时取 0），用于从样本推算总体。

**第三类：密度计算** — JOIN 多表 + 归一化常量

| 方法 | 指标 | 计算逻辑 |
|------|------|----------|
| `calcPedestrianDensity` | 客流密度 | `num / 14400 / cityTotalArea`（cityTotalArea = 1767984.22） |
| `calcResidentDensity` | 居住人口密度 | `num / 14400 / avgHomePerGrid` |
| `calcWorkingDensity` | 工作人口密度 | `num / 14400 / avgWorkPerGrid` |
| `calcWorkHolidayFlowRatio` | 工作日/节假日客流比 | `WROK_HOUR_FLOW / HOLIDAY_HOUR_FLOW` |

> 密度计算中常数 `14400` 代表每个网格的面积（120m × 120m）。

#### Service 层

| 类 | 类型 | 说明 |
|----|------|------|
| `RegionScoreDataService` | `abstract class` | 模板方法基类，定义 `listScoreByGridMap()`、`getScoreIndex()`、`getType()` 三个抽象方法，提供 `getResultMapByStatistics()` 公共方法用于结果组装 |
| `ExServiceImpl` | 实现类 | 当前仅计算年龄35-54占比（HOME 类型），类型标识 `"RR"`，得分索引 `0` |

---

## 开发环境配置

### 数据库配置（application.yml）

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://192.168.100.102:5432/inm
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
```

### 运行方式

```bash
# 开发运行 — 直接启动嵌入式 Tomcat
mvn spring-boot:run

# 打包部署
mvn package -DskipTests
java -jar target/mybatis-pg-demo-1.0-SNAPSHOT.jar
```

### 前置条件

- JDK 1.8+
- Maven 3.6+
- PostgreSQL 数据库实例（需提前建好表结构，参考 `create.txt` 中的 SQL 脚本）

---

## 扩展指南

### 新增一个人口画像指标

1. **`PopulationMapper.java`** — 添加接口方法，参数模板为 `gridList`、`date`、`populationType`（第一/二类）或带上额外参数（第三类密度）
2. **`PopulationMapper.xml`** — 编写对应 SQL，`resultType` 使用 `java.util.LinkedHashMap`，返回字段固定为 `code`（网格ID）+ `result`（计算值）
3. **`ConstantMapper.java/.xml`** — 如需要归一化常量，先在此添加计算方法
4. **Service 层** — 在 `ExServiceImpl`（或新建 Service）中调用新 Mapper 方法，通过 `getResultMapByStatistics()` 统一组装结果

### 新增 Score 类型（例如 "WW"）

新建 Service 类继承 `RegionScoreDataService`：

```java
@Service
public class WwServiceImpl extends RegionScoreDataService {

    @Autowired
    private PopulationMapper populationMapper;

    @Override
    public Map<String, List<String>> listScoreByGridMap(String date, List<String> gridIdList) {
        Map<String, List<String>> resultMap = new HashMap<>();
        getResultMapByStatistics(
            populationMapper.calcMaleRatio(gridIdList, date, "HOME"),
            resultMap, gridIdList
        );
        return resultMap;
    }

    @Override
    public Integer getScoreIndex() {
        return 1;              // 结果集中的位置下标
    }

    @Override
    public String getType() {
        return "WW";           // 类型标识符
    }
}
```

---

## 数据源表结构说明

| 表名 | 用途 |
|------|------|
| `user` | CRUD 演示表 |
| `tb_grid_pop_attr1` | 网格人口属性表（年龄分段、性别、学历、有车等基础属性） |
| `tb_grid_personalized_portrait` | 网格个性化画像表（有房、白领、中产、银发、金融理财、高消费、高净值等画像字段及比例） |
| `tb_grid_hour_flow` | 网格小时客流表（含 `type` 字段区分工作日 `WROK_HOUR_FLOW` / 节假日 `HOLIDAY_HOUR_FLOW`） |
| `tb_grid_permanent_num` | 网格常住人口数表（`population_type` 区分 HOME 居住 / WORK 工作） |

---

## 已知事项

- **`create.txt`**（4294行）包含了完整的 SQL 脚本（建表、密度计算、指标更新），是还原数据库结构的参考。项目启动**不会自动执行**该脚本——需要在 PostgreSQL 中手动执行或通过 Flyway / Liquibase 等迁移工具管理。
- **当前实现状态**：`ExServiceImpl` 仅实现了**年龄35-54占比**一个指标，其他指标的 Mapper 方法已全部就绪（`PopulationMapper.xml` 中的 SQL 已完成），等待被 Service 层调用接入。
- **数据库连接信息**（`application.yml`）当前为硬编码，建议通过环境变量或外部配置中心管理，避免敏感信息泄露。
